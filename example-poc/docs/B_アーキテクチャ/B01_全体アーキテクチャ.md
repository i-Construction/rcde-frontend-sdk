# B01: 全体アーキテクチャ

## 1. 構成図

```mermaid
flowchart TB
    subgraph browser ["ブラウザ"]
        LoginPage["Login Page"]
        ViewerPage["Viewer Page"]
        RCDEComp["RCDE コンポーネント"]
        Selector["ConstructionSelector"]
    end

    subgraph nextServer ["Next.js Server"]
        subgraph apiRoutes ["API Routes"]
            AuthToken["/api/auth/token\n2-legged トークン取得"]
            AuthCallback["/api/auth/callback\n3-legged コード交換"]
            ConstructionsAPI["/api/constructions\n現場・契約プロキシ"]
        end
        subgraph serverLib ["Server Libraries"]
            RCDEServer["rcde-server.ts\nSDK クライアント生成"]
            AuthStore["auth-store.ts\nCookie トークン管理"]
        end
    end

    subgraph external ["外部"]
        RCDEAPI["RCDE API"]
        RCDEOAuth["RCDE OAuth"]
    end

    LoginPage -->|"POST"| AuthToken
    LoginPage -->|"redirect"| RCDEOAuth
    RCDEOAuth -->|"callback"| AuthCallback
    AuthToken --> RCDEServer
    AuthCallback --> RCDEServer
    RCDEServer --> RCDEAPI
    AuthToken --> AuthStore
    AuthCallback --> AuthStore
    ConstructionsAPI --> RCDEServer

    ViewerPage -->|"SSR: トークン取得"| AuthStore
    ViewerPage --> RCDEComp
    ViewerPage --> Selector
    RCDEComp -->|"PCLoD タイル取得"| RCDEAPI
```

## 2. サーバーサイド / クライアントサイドの責任分界

| レイヤー | 責務 | アクセスできる情報 |
|---|---|---|
| **サーバーサイド**（API Route, SSR） | 認証トークン交換、`clientSecret` 保護、API プロキシ | `RCDE_CLIENT_SECRET`, Cookie |
| **クライアントサイド**（React） | UI 描画、RCDE コンポーネント操作、ユーザー操作 | `accessToken`（Cookie から取得）、公開環境変数 |

`clientSecret` はサーバーサイドの `process.env` にのみ存在し、ブラウザには一切送信されない。

## 3. SDK との統合ポイント

- **`RCDE` コンポーネント**: `ViewerClient.tsx` でラップし、`RCDEAppConfig` を渡す
- **`RCDEClient2Legged` / `RCDEClient3Legged`**: サーバーサイド（API Route）でのみ使用
- **PCLoD タイル取得**: RCDE コンポーネント内部で `accessToken` + `baseUrl` を使って直接 RCDE API を呼ぶ

参照: [SDK 01_architecture.md](../../../../my-document_rcde-frontend-sdk/機能一覧仕様書/01_architecture.md)
