import { FC } from "react";
import { RCDE, RCDEProps } from "../../src/components/RCDE";

const ContractDetail: FC<RCDEProps> = (props) => {
  return <RCDE {...props} />;
};

export { ContractDetail };
